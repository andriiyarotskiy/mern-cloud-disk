const initialState = {}

export const fileReducer = (state = initialState, action) => {
    switch (action.type) {
        case '': {
            return state
        }
        default :
            return state
    }
}